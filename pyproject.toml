[project]
name = "theclown"
version = "0.1.0"
description = "An unserious Python interpreter for a tiny subset of Rust"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tree-sitter>=0.25.2",
    "tree-sitter-rust>=0.24.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
